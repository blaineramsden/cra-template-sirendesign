import { useLayoutEffect } from "react";
import "../../scss/global.scss";
import "./home.scss";

const Home: React.FC = () => {

    useLayoutEffect(() => {
        window.scrollTo(0, 0)
    });
    

    

    return (
        <>
            
        </>
    );
};

export default Home;
